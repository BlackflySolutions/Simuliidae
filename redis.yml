# add in redis - this requires some supporting bits still
version: '3.4'
services:
  vredis:
    image: "blackflysolutions/redis"
    deploy:
      replicas: 1
      placement:
        constraints:
          - node.hostname=={VSITE_NODE-crosskeyellum}
      resources:
        limits:
          cpus: "1"
          memory: "120M"

